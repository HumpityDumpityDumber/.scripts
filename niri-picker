#!/usr/bin/env nu

let hex = niri msg --json pick-color |
from json |
get rgb |
each { |c|
    $c * 255 |
    math round |
    into binary -c |
    encode hex
} |
$"#($in.0)($in.1)($in.2)"

wl-copy $hex
notify-send --transient --app-name=niri-picker $hex
